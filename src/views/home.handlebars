<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The white store | Home</title>
</head>
<body>
    <div>
        <h1>The white store</h1>
        <ul>
        {{#each products}}
        <li>
            <h2>{{ this.title }}</h2>
            <p>Description: {{ this.description }}</p>
            <p>Price: ${{ this.price }}</p>
            <p>Stock: {{ this.stock }}</p>
            <hr>
        </li>
        {{/each}}
    </ul>
    </div>
</body>
</html>