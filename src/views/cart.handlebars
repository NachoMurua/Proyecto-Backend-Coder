<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The white store | Carrito</title>
</head>
<body>
    <h1>Carrito {{ cartId }}</h1>
    <table>
        <thead>
            <tr>
                <th>Productos</th>
                <th>Cantidad</th>
            </tr>
        </thead>
        <tbody>
            {{#each cartProducts}}
            <tr>
                <td>{{ this.product }}</td>
                <td>{{ this.quantity }}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>